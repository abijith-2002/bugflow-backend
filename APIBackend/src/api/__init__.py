"""
API package initialization for BugFlow backend.

This module marks `src.api` as a package and serves as the root for API modules
including configuration, routes, models, services, and database clients.
"""
# API package init
