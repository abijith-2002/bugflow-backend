# APIBackend runtime dependencies (FastAPI + supabase-py)
# Pin to a compatible, widely available set to avoid resolver conflicts and wheels missing on CI.

# Core ASGI stack
fastapi==0.110.3
starlette==0.37.2
uvicorn==0.30.6
h11==0.14.0
anyio==4.4.0
sniffio==1.3.1
typing_extensions==4.12.2

# Data validation
pydantic==2.11.3
pydantic_core==2.33.1
annotated-types==0.7.0
email_validator==2.2.0

# HTTP client stack (align with supabase deps)
httpx==0.27.2
httpcore==1.0.5
idna==3.7
certifi==2024.8.30

# Multipart/formdata
python-multipart==0.0.9

# Environment loading
python-dotenv==1.0.1

# Optional performance (ensure broad linux compatibility)
uvloop==0.19.0 ; platform_system != "Windows"
httptools==0.6.1 ; platform_system != "Windows"

# File watching for dev reload (uvicorn[standard] alternative)
watchfiles==0.21.0

# Supabase SDK (auth + db)
supabase==2.10.0
websockets==12.0  # compatible with realtime; avoid 15.x which can cause ABI/build issues

# JWT handling
PyJWT==2.9.0

# Development/test tooling (safe, but not required in production image)
flake8==7.1.1
pyflakes==3.2.0
pycodestyle==2.12.1
mccabe==0.7.0
pytest==8.3.2
packaging==24.1
iniconfig==2.0.0
pluggy==1.5.0
Pygments==2.18.0
Jinja2==3.1.4
MarkupSafe==2.1.5
mdurl==0.1.2
markdown-it-py==3.0.0
click==8.1.7
PyYAML==6.0.2
shellingham==1.5.4
rich==13.7.1
typing-inspection==0.4.0
typer==0.12.3
dnspython==2.6.1
